import { defineConfig } from 'tsup'
import { createDefaultOptions } from '@desselbane/configs/tsup'

export default defineConfig({
  ...createDefaultOptions(),
  platform: '{{platform}}',
  tsconfig: 'tsconfig.{{packageType}}.json',
})
